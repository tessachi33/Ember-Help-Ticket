import Ember from 'ember';

export default Ember.Route.extend({
  // We don't have anythign in here because save and delete
  // are being handled in the student.js and teacher.js routes respectively
});
