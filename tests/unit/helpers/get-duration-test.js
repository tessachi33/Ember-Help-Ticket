import { getDuration } from '../../../helpers/get-duration';
import { module, test } from 'qunit';

module('Unit | Helper | get duration');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = getDuration(42);
  assert.ok(result);
});
